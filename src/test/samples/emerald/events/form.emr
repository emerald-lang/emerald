html
  head
    title Form example

  body
    form#form-id
      h3 Name
      input (
        type text
        name name
        autocomplete off
      )

      h3 Email
      input (
        type text
        name email
        autocomplete off
      )

      h3 Message
      textarea (
        type text
        name message
        autocomplete off
      )

      button Submit (
        type submit
        click ->
          validateForm()
      )

    footer
      p Copyright Emerald Language 2016
