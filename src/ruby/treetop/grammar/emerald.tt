#!/usr/bin/env ruby

grammar Emerald
  include Tokens

  rule main
    temp / list / keyword space tag / keyword
  end

  rule temp
    keyword newline attr
  end

  rule list
    tag lparen attribute* rparen
  end

  rule attribute
    attr space* equals space* string
  end
end
